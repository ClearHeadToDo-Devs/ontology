@prefix actions: <https://clearhead.us/vocab/actions/v4#> .
@prefix cco: <https://www.commoncoreontologies.org/> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .

# =============================================================================
# Invalid Example: Invalid UUID Format
# =============================================================================
#
# This example should FAIL SHACL validation:
# UUID doesn't match proper UUID format pattern
#
# SHACL rule: actions:PlanUUIDShape should catch this
# =============================================================================

<urn:plan:bad-uuid> a cco:ont00000974 ;
    rdfs:label "Plan with invalid UUID" ;
    actions:hasUUID "not-a-valid-uuid-format" ;  # Invalid format
    cco:ont00001942 <urn:act:bad-uuid> .

<urn:act:bad-uuid> a cco:ont00000228 ;
    cco:ont00001868 actions:NotStarted .
